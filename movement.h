#ifndef MOVEMENT_H  // Continue if the header not defined 
#define MOVEMENT_H

#include <SDL2/SDL.h>

// Declare the function
void handleMovement(SDL_Event event, SDL_Rect &carProp, int speed);

#endif

